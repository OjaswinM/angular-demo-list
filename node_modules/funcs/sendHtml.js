fs = require('fs');


module.exports = function(res,path,contentType){
  console.log("Sending html");
  var head = {"Content-Type":contentType};
  res.writeHead(200,head);
  file = fs.createReadStream(path);
  file.pipe(res);
}
